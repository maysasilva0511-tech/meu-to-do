"use client"; import React, { useState } from 'react'; import { DashboardHeader } from '@/components/dashboard/DashboardHeader'; import { StatsCards } from '@/components/dashboard/StatsCards'; import { TaskList } from '@/components/tasks/TaskList'; import { useTasks } from '@/hooks/tasks'; import { supabase } from '@/lib/supabaseClient'; import { useNavigate } from 'react-router-dom'; export const Dashboard: React.FC = () => { const { tasks, isLoading, error } = useTasks(); const [showTaskModal, setShowTaskModal] = useState(false); const navigate = useNavigate(); // Converter tarefas para o formato correto const formattedTasks = tasks?.map(task => ({ ...task, created_at: task.created_at, updatedAt: task.updated_at || task.created_at, })) || []; // Calcular estatísticas const stats = { total: formattedTasks.length, completed: formattedTasks.filter(t => t.is_completed).length, pending: formattedTasks.filter(t => !t.is_completed).length, }; const handleCreateTask = (title: string) => { // Criar tarefa via hook // O hook já lida com o sucesso/erro e invalidação }; const handleLogout = async () => { try { await supabase.auth.signOut(); // Limpa estado local globalAuthState = { isAuthenticated: false, currentUser: null }; navigate('/auth/login'); } catch (error) { console.error('Erro ao fazer logout:', error); } }; return ( <div className="min-h-screen bg-gradient-to-br from-gray-50 to-gray-100"> <DashboardHeader onCreateTask={() => setShowTaskModal(true)} onLogout={handleLogout} /> <main className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8"> {/* Seção de Estatísticas */} <StatsCards stats={stats} /> {/* Seção de Lista de Tarefas */} <div className="mb-8"> <TaskList onCreateTask={handleCreateTask} /> </div> {/* Modal de criação de tarefas será adicionado em outro momento */} {/* Por enquanto, vamos criar a tarefa diretamente */} {showTaskModal && ( <div className="fixed inset-0 z-50 flex items-center justify-center bg-black bg-opacity-50"> <div className="bg-white rounded-xl p-6 w-full max-w-md"> <h3 className="text-lg font-semibold mb-4">Criar Nova Tarefa</h3> <div className="space-y-4"> <input type="text" placeholder="Digite o título da tarefa" className="w-full px-3 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-transparent" onKeyPress={(e) => { if (e.key === 'Enter' && e.currentTarget.value.trim()) { // Criar tarefa supabase .from('todos') .insert([{ title: e.currentTarget.value.trim(), user_id: 'mock-user-id', // Será obtido do usuário autenticado is_completed: false, created_at: new Date().toISOString() }]); setShowTaskModal(false); e.currentTarget.value = ''; } }} /> <div className="flex justify-end space-x-2"> <Button variant="outline" onClick={() => setShowTaskModal(false)} > Cancelar </Button> </div> </div> </div> </div> )} </main> </div> ); };